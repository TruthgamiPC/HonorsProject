After testing around the previous functions that were used for the desktop vpython app I found out that the key issues were with the "grpc" library and pinwheel functionality as it was out of date on the base version and needed an update and reconfiguration to handle server communication to google serices through local host. Issue is fixed for the moment and will track its working in case some errors occur. "cygrpc" is not compatable at highest version with the device it appears so if the error occurs again will have to investigate IoT and google cloud API calls instead of direct connections. Speed of that process is the same so performance wouldn't be affected only method of using